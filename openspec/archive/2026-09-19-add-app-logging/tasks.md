# Tasks: add-app-logging

## 1. Logger core
- [x] 1.1 Модель `AppLogLevel` (debug/info/error) и `LogEntry`.
- [x] 1.2 Интерфейс `AppLogger` (debug/info/error, stream, readAll, clear) + `NoopAppLogger`.
- [x] 1.3 `FileAppLogger`: запись всех уровней в файл, формат строки, сериализация записи.
- [x] 1.4 Ротация и лимит размера (порог файла, бэкапы, удаление старых).
- [x] 1.5 Чтение журнала (`readAll`) и очистка всех файлов.

## 2. Providers
- [x] 2.1 Провайдер `AppLogger` (файловый), каталог `<appSupport>/logs/`.
- [x] 2.2 Провайдер потока записей и состояния выбранного уровня отображения.
- [x] 2.3 Провайдер загрузки/обновления списка записей для экрана.

## 3. UI
- [x] 3.1 Экран `/logs`: список записей (время, уровень, сообщение), пустое состояние.
- [x] 3.2 Выбор уровня отображения (debug/info/error), фильтрация только показа.
- [x] 3.3 Обновление и очистка журнала (с подтверждением).
- [x] 3.4 Точка входа (меню на экране библиотеки) и маршрут.
- [x] 3.5 Строки локализации (ru/en).

## 4. Integration
- [x] 4.1 Логирование ошибок плеера и fallback на кэш.
- [x] 4.2 Логирование ошибок хранилища (подключение, доступ, импорт).
- [x] 4.3 Убедиться, что пароли и другие секреты не попадают в журнал.

## 5. Tests
- [x] 5.1 Unit: запись всех уровней в файл; выбранный уровень влияет только на отображение.
- [x] 5.2 Unit: ротация и лимит размера удаляют старейшие записи.
- [x] 5.3 Unit: `clear` удаляет все файлы, `readAll` возвращает записи в порядке времени.
- [x] 5.4 Widget: экран логов фильтрует записи по уровню и очищает журнал.
